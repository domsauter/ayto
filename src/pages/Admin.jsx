const Admin = () => {
  return (
    <div>
      <h1>Admin Page</h1>
      <p>This is where admins can generate general details.</p>
      {/* I will add the functionality to generate details later */}
    </div>
  );
};

export default Admin;
